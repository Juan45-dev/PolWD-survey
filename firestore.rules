rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only Cloud Functions (admin) can read/write. No client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
